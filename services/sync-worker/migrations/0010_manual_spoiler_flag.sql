-- الحرق يدويّ من الكاتب، لا مشتقٌّ من تقدّم القارئ.
--
-- `spoiler_after REAL` كان يحمل رقم الفصل الذي يُحجب التعليق قبله، فيُقارن
-- بتقدّم القارئ داخل VANTARA. وأسقط المالك هذا النموذج بسبب صحيح: القارئ قد
-- يكون أنهى العمل كله في موقع أو تطبيق آخر قبل أن ينتقل إلينا، فعدّادنا يحجب
-- عنه ما قرأه ويكشف لغيره ما لم يقرأه.
--
-- فصار القرار لمن يعرف وحده: كاتب التعليق يضغط «حرق»، والقارئ يكشفه بنفسه.
--
-- ولا بيانات إنتاج بعد، فالعمود القديم يُحذف بدل أن يبقى ميتًا مضلِّلًا:
-- عمودان للحرق يعنيان أن أحدهما سيُقرأ بالخطأ يومًا. وهو غير مفهرس، فحذفه
-- في SQLite مسموح.

ALTER TABLE comments ADD COLUMN spoiler INTEGER NOT NULL DEFAULT 0;
ALTER TABLE comments DROP COLUMN spoiler_after;
